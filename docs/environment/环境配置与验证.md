# C# / WPF 环境配置结果

2026-09-22 安装并验证完成。

## 已安装

| 项目 | 版本 / 路径 |
| --- | --- |
| Visual Studio Community 2026 | 18.7.3，`D:\VisualStudio2026\Products` |
| .NET 桌面开发工作负载 | `Microsoft.VisualStudio.Workload.ManagedDesktop` |
| .NET SDK（x64） | 10.0.301，`C:\Program Files\dotnet\sdk\10.0.301` |
| WPF / Windows Desktop 运行时 | 10.0.9 |
| WPF 编译目标包、C# 编译器、NuGet | 已随桌面开发组件安装 |
| Blend 界面设计工具 | `D:\VisualStudio2026\Products\Common7\IDE\Blend.exe` |

安装方式：通过本机 Microsoft 签名的 Visual Studio Installer，在现有 VS 实例中应用 `wpf.vsconfig`。使用 `--norestart`；完成后 vswhere 报告 `isComplete=true`、`isLaunchable=true`、`isRebootRequired=false`。

## 已完成验证

- `dotnet --info`：识别 SDK 10.0.301 和 WindowsDesktop 运行时 10.0.9。
- `dotnet new list wpf`：识别 WPF 应用、用户控件库、类库和自定义控件库模板。
- `dotnet build`：Release 构建成功，0 警告、0 错误。
- Visual Studio 自带 MSBuild：解决方案还原与 Debug 构建成功。
- 运行编译后的 WPF 程序：加载编译后的 XAML、执行按钮事件、更新数据绑定、生成 WPF 渲染图，结果 PASS。
- NuGet 官方 v3 包源连通性：HTTP 200。本次验证项目无第三方包依赖；未替未来串口助手安装具体业务库。

## 开始学习

使用 Visual Studio 打开：

`E:\TTPlayerSerialAss\tools\WpfEnvironmentCheck\WpfEnvironmentCheck.sln`

按 F5 运行，点击“测试 C# 事件与绑定”查看计数变化。编辑 `MainWindow.xaml` 练习布局，编辑 `MainWindow.xaml.cs` 练习 C# 事件处理。这是独立环境验证项目，串口助手应用尚未开始实现。

命令行启动：

```powershell
dotnet run --project E:\TTPlayerSerialAss\tools\WpfEnvironmentCheck\WpfEnvironmentCheck.csproj
```

自动重新验证：

```powershell
& E:\TTPlayerSerialAss\tools\WpfEnvironmentCheck\Verify-Wpf.ps1
```

相关记录：`dotnet-info.txt`、`visual-studio.json`、`vs-components-after.json`、`verification/build.log`、`verification/verification.json`、`verification/wpf-render.png`。

官方参考：[Visual Studio 安装命令参数](https://learn.microsoft.com/en-us/visualstudio/install/use-command-line-parameters-to-install-visual-studio?view=visualstudio)、[桌面开发工作负载](https://learn.microsoft.com/en-us/visualstudio/install/workload-component-id-vs-community?view=visualstudio)。
